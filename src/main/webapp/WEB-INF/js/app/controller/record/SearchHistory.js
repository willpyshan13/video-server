Ext.define('dy.controller.record.SearchHistory', {
    extend: 'Ext.app.Controller',
    views:["record.SearchHistory"],
    models:["Search"],
    stores:["Search"],
    init: function () {
        this.control({
        	
        });
    }
});