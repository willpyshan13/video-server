Ext.define('dy.controller.record.PlayCount', {
    extend: 'Ext.app.Controller',
    views:["record.PlayCount","record.PlayEditWindow"],
    models:["PlayCount"],
    stores:["PlayCount"],
    init: function () {
        this.control({
        	
        });
    }
});