Ext.define('dy.controller.record.PlayHistory', {
    extend: 'Ext.app.Controller',
    views:["record.PlayHistory"],
    models:["VideoHistory"],
    stores:["VideoHistory"],
    init: function () {
        this.control({
        	
        });
    }
});