Ext.define('dy.controller.news.BusinessTopline', {
    extend: 'Ext.app.Controller',
    views:["news.BusinessTopline","news.BusinessNewsEditWindow"],
    models:["BusinessNews"],
    stores:[],
    init: function () {
        this.control({
        	
        });
    }
});