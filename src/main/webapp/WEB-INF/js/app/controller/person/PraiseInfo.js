Ext.define('dy.controller.person.PraiseInfo', {
    extend: 'Ext.app.Controller',
    views:["person.PraiseInfo"],
    models:["Praise"],
    stores:["Praise"],
    init: function () {
        this.control({
        	
        });
    }
});