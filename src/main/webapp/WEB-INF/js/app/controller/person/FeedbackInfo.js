Ext.define('dy.controller.person.FeedbackInfo', {
    extend: 'Ext.app.Controller',
    views:["person.FeedbackInfo"],
    models:["Feedback"],
    stores:["Feedback"],
    init: function () {
        this.control({
        	
        });
    }
});