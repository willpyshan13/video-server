Ext.define('dy.controller.sys.UpdateInfo', {
    extend: 'Ext.app.Controller',
    views:["sys.UpdateInfo"],
    models:["UpdateInfo"],
    stores:["UpdateInfo"],
    init: function () {
        this.control({
        	
        });
    }
});