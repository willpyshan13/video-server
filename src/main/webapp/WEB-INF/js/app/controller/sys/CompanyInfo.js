Ext.define('dy.controller.sys.CompanyInfo', {
    extend: 'Ext.app.Controller',
    views:["sys.CompanyInfo"],
    models:["CompanyInfo"],
    stores:["CompanyInfo"],
    init: function () {
        this.control({
        	
        });
    }
});