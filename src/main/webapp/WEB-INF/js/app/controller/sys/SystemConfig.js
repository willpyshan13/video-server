Ext.define('dy.controller.sys.SystemConfig', {
    extend: 'Ext.app.Controller',
    views:["sys.SystemConfig"],
    stores:["SystemConfig"],
    init: function () {
        this.control({
        	
        });
    }
});