Ext.define('dy.controller.sys.Loading', {
    extend: 'Ext.app.Controller',
    views:["sys.Loading"],
    models:["Loading"],
    stores:["Loading"],
    init: function () {
        this.control({
        	
        });
    }
});