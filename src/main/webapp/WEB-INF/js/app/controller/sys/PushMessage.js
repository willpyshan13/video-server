Ext.define('dy.controller.sys.PushMessage', {
    extend: 'Ext.app.Controller',
    views:["sys.PushMessage"],
    models:["PushMessage"],
    stores:["PushMessage"],
    init: function () {
        this.control({
        	
        });
    }
});