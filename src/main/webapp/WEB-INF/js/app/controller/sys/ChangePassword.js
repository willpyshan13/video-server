Ext.define('dy.controller.sys.ChangePassword', {
    extend: 'Ext.app.Controller',
    views:["sys.ChangePassword"],
    models:["ChangePassword"],
    stores:["ChangePassword"],
    init: function () {
        this.control({
        	
        });
    }
});